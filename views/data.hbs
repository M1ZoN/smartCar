  <style>
  * {
  margin: 0;
  padding: 0;
  outline: none;
}

.container {
  height: 500px;
  width: 80%;
  margin: 30px auto;
}

.card {
  border-radius: 25px;
  box-shadow: -11px 11px 1px rgba(0, 0, 0, 0.3);
}

.card-head {
  
  height: 210px;
  background: #fa782e;
  /* Old browsers */
  background: -moz-linear-gradient(-45deg, #fa782e 8%, #c82930 83%);
  /* FF3.6-15 */
  background: -webkit-linear-gradient(-45deg, #fa782e 8%, #c82930 83%);
  /* Chrome10-25,Safari5.1-6 */
  background: linear-gradient(135deg, #fa782e 8%, #c82930 83%);
  /* W3C, IE10+, FF16+, Chrome26+, Opera12+, Safari7+ */
  filter: progid: DXImageTransform.Microsoft.gradient( startColorstr='#fa782e', endColorstr='#c82930', GradientType=1);
  /* IE6-9 fallback on horizontal gradient */
  border-radius: 25px 25px 0 0;
}

.card-logo {
  width: 55px;
  margin: 20px;
}

.back-text {
  display: inline-block;
  font-size: 125px;
  font-weight: 900;
  margin-left: -7px;
  margin-top: -15px;
  opacity: 0.1;
}

.product-detail {
  padding: 0 20px;
  font-size: 20px;
  color: #fff;
}
.product-title {
  padding: 20px 20px 5px 20px;
  display: block;
  font-size: 17px;
  font-weight: 500;
  letter-spacing: 1px;
  text-transform: uppercase;
}

.product-title b {
  font-weight: 900;
  letter-spacing: 0px;
}

#vehicles{
  background-color: radial-gradient(circle,#8c9ba5, #dfe3e6,#f5f7fa);
}
}
.product-caption {
  display: block;
  padding: 0 20px;
  font-size: 10px;
  font-weight: 400;
  text-transform: uppercase;
}

</style>
<div id="vehicles">
  <h3>Step 3: Retrieve data</h3>
  <p>
    Success! You successfully sent a {{type}} request to the {{vehicle.year}} {{vehicle.make}} {{vehicle.model}}.
  </p>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Data</th>
        <th scope="col">Value</th>
      </tr>
    </thead>
    <tbody>
      {{#each data}}
        <tr>
          <td>{{@key}}</td>
          <td>{{this}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <div>
    <a href="/vehicles" class="btn btn-primary" role="button">Make another request</a>
    <a href="/logout" class="btn btn-link" role="button">Logout</a>
  </div>
</div>
<div class="container">
  <div class="card">
    <div class="card-head">
      <img src="https://www.nurun.com/media/cache/caseStudy_show_client/upload/client/logo/54622c24a37b4.png" alt="logo" class="card-logo">
      <div class="product-detail">
        <h2>{{vehicle.model}}</h2> Safety is the most important part of the overall {{vehicle.model}} design.
      </div>
      <span class="back-text">
              ZERO EMISSION
            </span>
    </div>
  </div>
</div>
